import React from "react";

const page = ({ params }: { params: { ruta_dinamica: string } }) => {
  return (
    <div className="h-[30vh] flex items-center justify-center">
      
    </div>
  );
};
export default page;
